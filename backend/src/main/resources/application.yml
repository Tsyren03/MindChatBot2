server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  messages:
    basename: messages
    encoding: UTF-8
    fallback-to-system-locale: false
  thymeleaf:
    encoding: UTF-8
    cache: false
  data:
    mongodb:
      uri: "mongodb://localhost:27017/mindChatBotDB"
  mail:
    host: "smtp.gmail.com"
    port: 587
    username: mindchatbotst@gmail.com
    password: jfsauoccofnsufrh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  web:
    resources:
      static-locations: "classpath:/static/"

openai:
  api:
  model: "gpt-4o-mini"
  system:
    prompt: |
      You are a kind, emotionally smart mental health assistant.
      Keep replies short, casual, and friendly—like social media messages.
      Listen well and respond with empathy without repeating yourself.
      Encourage sharing, but don’t push.

jwt:

logging:
  level:
    org.springframework.security: INFO
    MindChatBot.mindChatBot: INFO